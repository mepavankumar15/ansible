- name: play level variables
  hosts: local
  connection: local
  vars_files:
  - course.yaml #it is used to mention a file   
  tasks:
  - name: print variables
    ansible.builtin.debug:
      msg: "Course is {{ COURSE }} , trainer is {{ TRAINER }}"